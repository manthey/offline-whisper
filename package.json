{
  "name": "offline-whisper",
  "version": "0.0.0",
  "scripts": {
    "build": "node build.js",
    "tests": "npm run build && node tests/test_desktop.js && node tests/test_mobile.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "playwright": "^1.60.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
